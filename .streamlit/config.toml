[server]
# Default port. start.sh and share.sh both bind to this.
# When you run share.sh, headless mode is set explicitly on the command line.
port = 8501

[browser]
# Don't phone home with usage stats.
gatherUsageStats = false

[theme]
# Dark theme so the header banner can fade to the page background seamlessly.
base = "dark"
